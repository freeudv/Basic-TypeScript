"use strict";
// Аннотации функций
function add(x, y) {
    return x + y;
}
// Так выглядит полное описание стрелочной функции
var arrowAdd = function (x, y) { return x + y; };
// Так как параметы повторяются, можно указывать только водном месте.
var arrowAdd1 = function (x, y) { return x + y; };
//Или
var arrowAdd2 = function (x, y) { return x + y; };
var arrowAdd3 = function (x, y) { return x + y; };
var handler = function (req, res, next) { };
